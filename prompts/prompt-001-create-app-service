create the bicep code needed to deploy an app service instance to azure on a low cost development SKU in UKSOUTH. make sure system assigned managed identity is enabled and store the value of the 
Object (principal) ID for use in other tasks

